# Copy to deploy/.env and fill in before `docker compose up -d`

# Your domain — becomes your Matrix server name (user IDs look like @you:chat.example.com)
SYNAPSE_SERVER_NAME=chat.example.com

# Default homeserver shown on the web client's login screen
VITE_DEFAULT_HOMESERVER=https://chat.example.com
